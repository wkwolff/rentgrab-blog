---
import Logo from '@/components/Logo.astro';

const currentYear = new Date().getFullYear();

const footerLinks = {
  'Blog Categories': [
    { href: '/blog/getting-started', label: 'Getting Started' },
    { href: '/blog/maximizing-earnings', label: 'Maximize Earnings' },
    { href: '/blog/smart-renting', label: 'Smart Renting' },
    { href: '/blog/success-stories', label: 'Success Stories' },
    { href: '/blog/sustainability-impact', label: 'Sustainability' },
  ],
  'RentGrab': [
    { href: 'https://rentgrab.com', label: 'Main Platform', external: true },
    { href: 'https://rentgrab.com/about', label: 'About Us', external: true },
    { href: '/contact', label: 'Contact Us' },
  ],
  'Resources': [
    { href: '/rss.xml', label: 'RSS Feed' },
    { href: '/sitemap.xml', label: 'Sitemap' },
    { href: '/privacy', label: 'Privacy Policy' },
    { href: '/terms', label: 'Terms of Service' },
  ],
};

const socialLinks = [
  {
    name: 'Facebook',
    href: 'https://www.facebook.com/rentgrab',
    icon: `<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/></svg>`,
  },
  {
    name: 'X',
    href: 'https://x.com/rentgrab',
    icon: `<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/></svg>`,
  },
  {
    name: 'LinkedIn',
    href: 'https://www.linkedin.com/company/rentgrab',
    icon: `<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M19 0h-14c-2.761 0-5 2.239-5 5v14c0 2.761 2.239 5 5 5h14c2.762 0 5-2.239 5-5v-14c0-2.761-2.238-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.268c-.966 0-1.75-.79-1.75-1.764s.784-1.764 1.75-1.764 1.75.79 1.75 1.764-.783 1.764-1.75 1.764zm13.5 12.268h-3v-5.604c0-3.368-4-3.113-4 0v5.604h-3v-11h3v1.765c1.396-2.586 7-2.777 7 2.476v6.759z"/></svg>`,
  },
  {
    name: 'Instagram',
    href: 'https://www.instagram.com/rent.grab',
    icon: `<svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zM5.838 12a6.162 6.162 0 1 1 12.324 0 6.162 6.162 0 0 1-12.324 0zM12 16a4 4 0 1 1 0-8 4 4 0 0 1 0 8zm4.965-10.405a1.44 1.44 0 1 1 2.881.001 1.44 1.44 0 0 1-2.881-.001z"/></svg>`,
  },
];
---

<footer class="bg-neutral-50 border-t border-neutral-200 mt-16">
  <div class="container-lg py-12">
    <!-- Top Section -->
    <div class="grid grid-cols-1 md:grid-cols-4 gap-8 mb-8">
      <!-- Brand -->
      <div class="md:col-span-1">
        <div class="flex items-center gap-3 mb-4">
          <Logo />
          <span class="text-xl font-semibold text-neutral-900">Blog</span>
        </div>
        <p class="text-small text-neutral-600 mb-4">
          Join the sharing economy. Turn idle items into income and save money by renting from neighbors.
        </p>
        <!-- Social Links -->
        <div class="flex gap-3">
          {socialLinks.map((social) => (
            <a
              href={social.href}
              target="_blank"
              rel="noopener noreferrer"
              class="text-neutral-500 hover:text-primary-500 transition-colors"
              aria-label={`Follow us on ${social.name}`}
            >
              <Fragment set:html={social.icon} />
            </a>
          ))}
        </div>
      </div>
      
      <!-- Links -->
      {Object.entries(footerLinks).map(([category, links]) => (
        <div>
          <h3 class="font-semibold text-neutral-900 mb-4">{category}</h3>
          <ul class="space-y-2">
            {links.map((link) => (
              <li>
                <a
                  href={link.href}
                  target={link.external ? '_blank' : undefined}
                  rel={link.external ? 'noopener noreferrer' : undefined}
                  class="text-small text-neutral-600 hover:text-primary-500 transition-colors"
                >
                  {link.label}
                  {link.external && (
                    <span class="inline-block ml-1">↗</span>
                  )}
                </a>
              </li>
            ))}
          </ul>
        </div>
      ))}
    </div>
    
    <!-- Newsletter Signup -->
    <div class="border-t border-neutral-200 pt-8 mb-8">
      <div class="max-w-md">
        <h3 class="font-semibold text-neutral-900 mb-2">Join the RentGrab Waitlist</h3>
        <p class="text-small text-neutral-600 mb-4">
          Be first to know when we launch. Get exclusive early access benefits.
        </p>
        <form class="flex gap-2" id="footer-newsletter-form">
          <input
            type="email"
            name="email"
            placeholder="Enter your email"
            required
            class="flex-1 px-4 py-2 border border-neutral-200 rounded-md focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-transparent"
            aria-label="Email address"
          />
          <button type="submit" class="btn-primary">
            Subscribe
          </button>
        </form>
      </div>
    </div>
    
    <!-- Bottom Section -->
    <div class="border-t border-neutral-200 pt-8">
      <div class="flex flex-col sm:flex-row justify-between items-center gap-4">
        <p class="text-small text-neutral-600">
          © {currentYear} RentGrab. All rights reserved.
        </p>
        <p class="text-small text-neutral-500">
          Built with ❤️ for the rental community
        </p>
      </div>
    </div>
  </div>
</footer>

<script>
  // HubSpot configuration
  const HUBSPOT_PORTAL_ID = '50196841';
  const HUBSPOT_FORM_ID = '8a3b20e8-b5ed-46ea-b35d-ef9f538b0542';
  
  // Helper function to get cookies
  function getCookie(name: string): string | null {
    const value = `; ${document.cookie}`;
    const parts = value.split(`; ${name}=`);
    if (parts.length === 2) {
      return parts.pop()?.split(';').shift() || null;
    }
    return null;
  }
  
  // Footer newsletter form handler
  const footerForm = document.getElementById('footer-newsletter-form') as HTMLFormElement;
  
  if (footerForm) {
    footerForm.addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const formData = new FormData(footerForm);
      const email = formData.get('email') as string;
      const button = footerForm.querySelector('button') as HTMLButtonElement;
      const originalButtonText = button?.textContent || 'Subscribe';
      
      // Show loading state
      if (button) {
        button.disabled = true;
        button.textContent = 'Subscribing...';
      }
      
      try {
        // Prepare HubSpot submission data
        const hubspotData = {
          fields: [
            { name: 'email', value: email }
          ],
          context: {
            pageUri: window.location.href,
            pageName: document.title
          }
        };
        
        // Add HubSpot tracking cookie if available
        const hutk = getCookie('hubspotutk');
        if (hutk) {
          (hubspotData.context as any).hutk = hutk;
        }
        
        // Submit to HubSpot
        const response = await fetch(
          `https://api.hsforms.com/submissions/v3/integration/submit/${HUBSPOT_PORTAL_ID}/${HUBSPOT_FORM_ID}`,
          {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json'
            },
            body: JSON.stringify(hubspotData)
          }
        );
        
        if (response.ok) {
          // Show success message
          if (button) {
            button.textContent = '✓ Subscribed!';
            button.classList.add('bg-green-600', 'hover:bg-green-700');
            button.classList.remove('bg-primary-500', 'hover:bg-primary-600');
          }
          
          // Clear form
          footerForm.reset();
          
          // Reset button after delay
          setTimeout(() => {
            if (button) {
              button.textContent = originalButtonText;
              button.classList.remove('bg-green-600', 'hover:bg-green-700');
              button.classList.add('bg-primary-500', 'hover:bg-primary-600');
              button.disabled = false;
            }
          }, 3000);
        } else {
          throw new Error('Subscription failed');
        }
      } catch (error) {
        // Show error message
        console.error('Footer newsletter signup error:', error);
        if (button) {
          button.textContent = 'Error - Try again';
          button.classList.add('bg-red-600', 'hover:bg-red-700');
          button.classList.remove('bg-primary-500', 'hover:bg-primary-600');
        }
        
        // Reset button after delay
        setTimeout(() => {
          if (button) {
            button.textContent = originalButtonText;
            button.classList.remove('bg-red-600', 'hover:bg-red-700');
            button.classList.add('bg-primary-500', 'hover:bg-primary-600');
            button.disabled = false;
          }
        }, 3000);
      }
    });
  }
</script>